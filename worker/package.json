{
  "name": "course-renderer",
  "version": "1.0.0",
  "description": "Cloudflare Worker for rendering course content",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node test_comprehensive_worker.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240815.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.71.0"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
